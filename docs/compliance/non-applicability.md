# Documented non-applicability rationale

This record backs the `approved` status of authority-register categories whose
resolution is that the concern does not apply to this project. It is a factual
attestation by the project owner, not legal advice.

## employer-contractor-predecessor-c0re-permissions — non-applicable

The project's original code was authored by the sole owner (`w0rldx`) on their
own time and equipment. It is **not** subject to any employment IP-assignment
clause, client/contractor agreement, or predecessor-project (including "C0re")
license or ownership claim. Repository git history shows a single author
identity. No third-party permission is therefore required to publish the
project-authored material under Apache-2.0.

## copied-adapted-materials — non-applicable (with carve-out)

Aside from the vendored `agency-agents` persona templates — which are accounted
for separately under the `vendored-agency-agents` category (MIT, provenance and
license retained in `XE-Local-AI-Engine.Client.Application/Services/Agents/Templates/`)
— all schemas, database migrations, golden/eval vectors, and code/prompt
templates in this repository are original authorship by the project owner. No
other externally copied or adapted material is redistributed.
