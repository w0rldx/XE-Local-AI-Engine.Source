<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>XE_Local_AI_Engine.Client.Testing</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- A test-only catalog document, embedded so every consumer reads the same bytes regardless of its own
         working directory. It is never shipped and no catalog build produces it. -->
    <EmbeddedResource Include="ExternalApps\sample-catalog-manifest.json"/>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions"/>
    <PackageReference Include="Scrutor"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\XE-Local-AI-Engine.Client\XE-Local-AI-Engine.Client.csproj"/>
    <ProjectReference Include="..\XE-Local-AI-Engine.Client.Application\XE-Local-AI-Engine.Client.Application.csproj"/>
  </ItemGroup>

</Project>
