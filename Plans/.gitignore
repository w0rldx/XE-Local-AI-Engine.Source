# Local planning and session artifacts are intentionally untracked.
# Ignore everything in Plans/ except this file so machine-specific working
# notes do not become durable repository authorities.
*
!.gitignore
